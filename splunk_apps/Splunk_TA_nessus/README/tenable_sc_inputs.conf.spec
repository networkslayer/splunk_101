[<input_name>]
server = server name in tenable_sc_servers.conf
data = data to fetch, which can be sc_vulnerability
start_time = the start time to fetch the data
index = index of the fetched data
interval = input interval in seconds
batch_size = the size of events collected for each request
